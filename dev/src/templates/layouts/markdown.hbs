{{> head }}

<div class="container">
    <main class="main" id="main" data-page="{{this.pageid}}">
        <div class="content" id="content">
            <section>
                {{#markdown}}
                    {{> body}}
                {{/markdown}}
            </section>
        </div>
    </main>
</div>

{{> nav}}

{{> bottom }}
